# Adding Alliances

Alliance files are automatically loaded from:

```text
addon/convergence_galaxy/lua/convergence/alliances/definitions/
```

A faction can belong to one alliance at a time.

Supported relationship values:

```text
allied
neutral
hostile
```

Faction IDs and alliance IDs must remain stable after persistent campaign data
starts referencing them.
